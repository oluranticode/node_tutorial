//Modules
const names = require('./names');
//    console.log(names);
const sayHi = require('./utils');
const name = require('./alternative-flavor');
require('./mind-grenade');
    sayHi('Susan');
    sayHi(names.Tope);
    sayHi(names.Shade);